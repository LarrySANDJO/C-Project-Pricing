#pragma once

/* =========================================================
   ENUM : TYPE D'OPTION
   ========================================================= */
enum class OptionType
{
    Call,
    Put
};
